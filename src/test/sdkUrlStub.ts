// Stub de los imports `?url` de los SDKs de DigitalPersona para vitest —
// ver el alias en vitest.config.ts. Exporta un string, igual que `?url`
// en el build real. Los tests pre-marcan el <script data-tinta-bio> de
// esta URL como cargado y aportan window.Fingerprint con un mock.
export default "dp-sdk-stub.js";
